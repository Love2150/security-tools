[project]
name = "pcap-quick-profiler"
version = "0.1.0"
description = "Fast PCAP triage with TLS/DNS focus and beaconing suspects"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pyshark>=0.6", "jinja2>=3.1"]

[project.scripts]
pcap-profiler = "pcap_profiler:main"
